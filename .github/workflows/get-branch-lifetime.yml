on:
  push:

jobs:
  get-branch-lifetime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: get first commit datetime
        run: |
          git status
          git log
